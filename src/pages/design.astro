---
/**
 * La idea que pidió esta sección la llamaba /design. La sección vive en
 * /diseno, como el resto de rutas de la web, así que el nombre en inglés se
 * queda solo como puerta.
 */
return Astro.redirect('/diseno', 302);
---
